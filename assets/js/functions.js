// Generate class with a random number to avoid ad blockers :(

$(document).ready(function (){
	var number = Math.floor(Math.random() * 999999) + 100000;
  $('#screw-abp').addClass('rennab--' + number);
});
